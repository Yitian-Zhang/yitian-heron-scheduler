2018-05-15 add ------------------------
add:
    LocalKey
    LocalContext
    LocalLauncher

add package:
    com.twitter.heron.scheduler (all)

pom.xml add:
    <heron custom scheduler> as follows at this pom.xml

Scheduler:
    LocalTriggerScheduler

---------------------------------------

2018-05-16 add ------------------------
add only at this project:
    AssignmentTracker.java